{
  "name": "v2-addon",
  "version": "0.0.0",
  "private": true,
  "repository": "",
  "license": "MIT",
  "author": "",
  "scripts": {
    "build": "pnpm --filter v2-addon build",
    "lint": "pnpm --filter '*' lint",
    "lint:fix": "pnpm --filter '*' lint:fix",
    "prepare": "pnpm build",
    "start": "concurrently 'npm:start:*' --restart-after 5000 --prefix-colors cyan,white,yellow",
    "start:addon": "pnpm --filter v2-addon start --no-watch.clearScreen",
    "start:embroider-app": "pnpm --filter embroider-app start",
    "test": "pnpm --filter '*' test"
  },
  "devDependencies": {
    "concurrently": "^7.2.1",
    "prettier": "^2.5.1"
  },
  "pnpm": {
    "overrides": {
      "@types/eslint": "^7.0.0"
    },
    "patchedDependencies": {
      "@embroider/webpack@2.1.1": "patches/@embroider__webpack@2.1.1.patch",
      "ember-auto-import@2.6.1": "patches/ember-auto-import@2.6.1.patch"
    }
  }
}
