import templateOnly from '@ember/component/template-only';

import type { TOC } from '@ember/component/template-only';

export default templateOnly() as TOC<{ Args: { class: string }, Element: HTMLParagraphElement }>;
